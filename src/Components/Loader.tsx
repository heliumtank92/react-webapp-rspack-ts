import { DsLoader } from '@am92/react-design-system'
import React, { PureComponent } from 'react'
// import { DsLoader } from '@am92/react-design-system'

export default class Loader extends PureComponent {
  render() {
    return (
      // <DsLoader ds-variant="threeDot" position="fixed" color="actionPrimary" />
      <>Loading...</>
    )
  }
}
